const getResultTime = (state) => state.resultTime;
const getTime = (state) => state.time;
const getDefaultTime = (state) => state.defaultTime;

export default {
  getResultTime,
  getTime,
  getDefaultTime,
};
